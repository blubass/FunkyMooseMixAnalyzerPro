# Mix Analyzer App (ASCII-safe)

Requirements:
- Python 3.9+
- ffmpeg in PATH
- pip install numpy matplotlib

Run (dev):
- macOS: python3 app.py
- Windows: python app.py

Build (PyInstaller):
- pip install pyinstaller
- pyinstaller --noconfirm --windowed --name "Mix Analyzer" app.py
